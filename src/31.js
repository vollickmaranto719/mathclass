function getRandomInt(max) {
  return Math.floor(Math.random() * Math.floor(max));
}

// Example usage:

const maxNumber = 100;
console.log(getRandomInt(maxNumber)); // Output: random number between 0 and maxNumber - 1
