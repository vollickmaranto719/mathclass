function getRandomInt(max) {
  return Math.floor(Math.random() * max);
}

// Example usage:
let randomNumber = getRandomInt(10) + getRandomInt(5); // Generate a random number between 10 and 15
console.log(randomNumber);
